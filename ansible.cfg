[defaults]
# Lets playbooks/rotate.yml find roles/ssh_key_rotation when this repo is run directly
# (e.g. ansible-playbook playbooks/rotate.yml), without installing it as a collection first.
roles_path = ./roles
